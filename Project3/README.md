The dependencies required to run the files are as follows, cvxopt , Matplotlib,  and Numpy.

Run each file in terminal ("python foe_q.py") , after it's finished it should end with a plot of the result.
